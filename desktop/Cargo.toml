[package]
name = "desktop"
version = "0.1.0"
edition = "2024"

[dependencies]
chip8_core = { path = "../chip8_core" }
winit = { version = "^0.30.13", default-features = false, features = [
    "wayland",
    "wayland-dlopen",
    "x11",
    "rwh_06",
] }
pixels = "^0.16.0"